<script>
  import "../app.css";
  import Header from "../components/Header.svelte";
  import Footer from "../components/Footer.svelte";
  let y=0;
  let innerHeight = 0;
  let innerWidth = 0;

  function goTop(){
    document.body.scrollIntoView({behavior: "smooth", block: "start", inline: "nearest"});
  }
</script>

<div class="relative flex flex-col mx-auto w-full 
text-sm sm:text-base min-h-screen ">
  <div class={"fixed bottom-0 w-full duration-75 flex p-10 z-10"
    + (y>0 ? " opacity-full pointer-events-auto " : "pointer-events-none opacity-0")}
  >
  <button on:click={goTop} class="ml-auto rounded-full bg-gray-200 text-violet-400 px-2 py-1 sm:px-4 sm:py-4 hover:bg-slate-600 cursor-pointer">
    <i class="fa-solid fa-arrow-up grid place-items-center aspect-square text-xl"></i>
  </button>
  </div>
  <Header y={y}/>
  
  <slot />
  <Footer />

</div>
<svelte:window bind:scrollY={y}  />
